in command line:

1) npm install gulp
	// you will intall gulp and all dependencies, witch you can see in package.json of root folder of the project

2) gulp watch
	// you will start watching sass(css) files